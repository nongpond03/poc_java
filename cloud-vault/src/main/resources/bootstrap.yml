spring:
  config:
    import: vault://
  application:
    name: fcd
  cloud:
    vault:
      application-name: fcd
      host: localhost
      port: 8200
      scheme: http
      uri: http://localhost:8200
      authentication: TOKEN
      token: hvs.z1J4SDR92UVi7KpQHAW2cGYD
      kv:
        enabled: true
        backend: finnet
        profile-separator: '/'
        default-context: application
